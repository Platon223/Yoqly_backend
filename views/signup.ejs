<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign Up | DarkSocial</title>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      :root {
        --gradient-start: #8e2de2;
        --gradient-end: #ff6ec4;
        --bg: #121212;
        --card: #1f1f1f;
        --text: #f1f1f1;
      }

      * {
        box-sizing: border-box;
      }

      body {
        background-color: var(--bg);
        margin: 0;
        font-family: "Segoe UI", sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        padding: 15px;
      }

      .form-container {
        background-color: var(--card);
        padding: 40px 35px;
        border-radius: 15px;
        box-shadow: 0 0 20px #00000050;
        width: 100%;
        max-width: 420px;
      }

      h2 {
        text-align: center;
        background: linear-gradient(
          to right,
          var(--gradient-start),
          var(--gradient-end)
        );
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        font-size: 26px;
        margin-bottom: 25px;
      }

      input[type="text"],
      input[type="email"],
      input[type="password"] {
        width: 100%;
        padding: 12px 18px;
        margin-bottom: 20px;
        border: none;
        border-radius: 8px;
        background-color: #2a2a2a;
        color: var(--text);
        font-size: 14px;
      }

      input::placeholder {
        color: #aaa;
      }

      .file-upload {
        margin-bottom: 20px;
      }

      .file-upload label {
        display: block;
        background: linear-gradient(
          to right,
          var(--gradient-start),
          var(--gradient-end)
        );
        color: white;
        padding: 10px 15px;
        border-radius: 8px;
        cursor: pointer;
        text-align: center;
        transition: opacity 0.2s ease;
      }

      .file-upload label:hover {
        opacity: 0.9;
      }

      .file-upload input {
        display: none;
      }

      button {
        width: 100%;
        padding: 12px;
        background: linear-gradient(
          to right,
          var(--gradient-start),
          var(--gradient-end)
        );
        border: none;
        border-radius: 8px;
        color: white;
        font-size: 16px;
        font-weight: bold;
        cursor: pointer;
        transition: transform 0.2s ease;
      }

      button:hover {
        transform: scale(1.02);
      }

      .footer {
        margin-top: 15px;
        text-align: center;
        font-size: 13px;
        color: #aaa;
      }

      .footer a {
        color: var(--gradient-end);
        text-decoration: none;
      }

      .footer a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div class="form-container">
      <h2>Create an Account</h2>

      <h4 id="loading-error"></h4>

        <input id="name" type="text" placeholder="Full Name" required />
        <input id="username" type="text" placeholder="Username" required />
        <input id="email" type="email" placeholder="Email" required />
        <input id="interest" type="text" placeholder="Main Interest" required />
        <input id="password" type="password" placeholder="Password" required />
        <input
          id="confirmed-password"
          type="password"
          placeholder="Confirm Password"
          required
        />

        <div class="file-upload">
          <label for="profile-pic"
            ><i class="fa-solid fa-image"></i> Upload Profile Photo</label
          >
          <input type="file" id="profile-pic" accept="image/*" />
        </div>

        <button class="signup-button" >Sign Up</button>
      </form>
      <div class="footer">
        Already have an account? <a href="login.html">Login</a>
      </div>
    </div>
    <script src="https://cdn.socket.io/4.0.1/socket.io.min.js"></script>
    <script src="/signup.js"></script>
  </body>
</html>
