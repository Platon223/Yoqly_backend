<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DarkSocial</title>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      :root {
        --gradient-start: #8e2de2;
        --gradient-end: #ff6ec4;
        --bg: #121212;
        --card: #1e1e1e;
        --text: #f1f1f1;
        --muted: #bbbbbb;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Segoe UI", sans-serif;
      }

      body {
        background: var(--bg);
        color: var(--text);
        display: flex;
        height: 100vh;
        overflow: hidden;
      }

      .sidebar {
        width: 80px;
        background: linear-gradient(
          to bottom,
          var(--gradient-start),
          var(--gradient-end)
        );
        display: flex;
        flex-direction: column;
        align-items: center;
        padding-top: 20px;
        transition: left 0.3s ease;
        z-index: 1001;
        position: fixed;
        top: 0;
        bottom: 0;
        left: 0;
      }

      .sidebar i {
        font-size: 22px;
        margin: 25px 0;
        color: #f8f8f8;
        transition: transform 0.3s, color 0.3s;
        cursor: pointer;
      }

      .sidebar i:hover {
        transform: scale(1.2);
        color: #ffffff;
        text-shadow: 0 0 8px #ffffffaa;
      }

      .main {
        flex: 1;
        display: flex;
        flex-direction: column;
        margin-left: 80px;
        position: relative;
        height: 100vh;
        width: calc(100% - 80px);
      }

      .topbar {
        height: 60px;
        background-color: #1c1c1c;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 20px;
        border-bottom: 1px solid #333;
        box-shadow: 0 2px 8px #00000050;
        position: relative;
        z-index: 900;
      }

      .topbar h1 {
        font-size: 22px;
        background: linear-gradient(
          to right,
          var(--gradient-start),
          var(--gradient-end)
        );
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
      }

      .menu-btn {
        display: none;
        font-size: 22px;
        color: #fff;
        cursor: pointer;
      }

      .search-bar input {
        padding: 10px 18px;
        background: #222;
        border: none;
        border-radius: 30px;
        color: #fff;
        outline: none;
        transition: 0.3s;
        box-shadow: 0 0 10px #00000040;
      }

      .search-bar input:focus {
        box-shadow: 0 0 10px var(--gradient-end), 0 0 20px var(--gradient-start);
        border: 1px solid var(--gradient-end);
      }

      .feed {
        flex: 1;
        overflow-y: auto;
        padding: 20px;
        padding-bottom: 80px;
        animation: fadeIn 0.8s ease-in-out;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .post {
        background-color: var(--card);
        padding: 15px;
        margin-bottom: 20px;
        border-radius: 15px;
        box-shadow: 0 0 10px #00000030;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      .post:hover {
        transform: translateY(-6px);
        box-shadow: 0 0 20px #ff6ec440;
      }

      .post h3 {
        margin-bottom: 5px;
        background: linear-gradient(
          to right,
          var(--gradient-start),
          var(--gradient-end)
        );
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
      }

      .post p {
        color: var(--muted);
        margin-bottom: 10px;
      }

      .post img {
        width: 100%;
        max-height: 200px;
        object-fit: cover;
        border-radius: 10px;
        transition: transform 0.3s ease;
      }

      .post img:hover {
        transform: scale(1.02);
      }

      .post-actions {
        margin-top: 12px;
        display: flex;
        gap: 20px;
        font-size: 16px;
        color: var(--muted);
      }

      .post-actions i:hover {
        color: var(--gradient-end);
        cursor: pointer;
        transform: scale(1.2);
        transition: 0.2s ease;
      }

      .create-btn {
        position: absolute;
        bottom: 25px;
        right: 25px;
        background: linear-gradient(
          to right,
          var(--gradient-start),
          var(--gradient-end)
        );
        color: #fff;
        border: none;
        border-radius: 50px;
        padding: 12px 20px;
        font-size: 16px;
        cursor: pointer;
        box-shadow: 0 0 15px #ff6ec450;
        transition: 0.3s ease;
        z-index: 10;
      }

      .create-btn:hover {
        transform: scale(1.05);
        box-shadow: 0 0 20px #ff6ec4aa;
      }

      .overlay {
        display: none;
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.6);
        z-index: 1000;
      }

      ::-webkit-scrollbar {
        width: 8px;
      }

      ::-webkit-scrollbar-thumb {
        background: #333;
        border-radius: 10px;
      }

      ::-webkit-scrollbar-thumb:hover {
        background: #444;
      }

      /* Responsive */
      @media (max-width: 768px) {
        body {
          flex-direction: column;
        }

        .sidebar {
          left: -80px;
        }

        .sidebar.open {
          left: 0;
        }

        .main {
          margin-left: 0;
          width: 100%;
        }

        .topbar {
          flex-direction: column;
          align-items: flex-start;
          padding: 10px;
        }

        .topbar h1 {
          font-size: 18px;
        }

        .menu-btn {
          display: block;
        }

        .search-bar {
          width: 100%;
          margin-top: 10px;
        }

        .search-bar input {
          width: 100%;
        }

        .create-btn {
          bottom: 80px;
          right: 15px;
          padding: 10px 16px;
          font-size: 14px;
        }

        .overlay.show {
          display: block;
        }
      }
    </style>
  </head>
  <body>
    <div class="overlay" id="overlay" onclick="closeSidebar()"></div>

    <div class="sidebar" id="sidebar">
      <i class="fas fa-home"></i>
      <i class="fas fa-user-friends"></i>
      <i class="fas fa-bell"></i>
      <i class="fas fa-envelope"></i>
      <i class="fas fa-cog"></i>
    </div>

    <div class="main">
      <div class="topbar">
        <div style="display: flex; align-items: center; gap: 15px">
          <i class="fas fa-bars menu-btn" onclick="toggleSidebar()"></i>
          <h1>DarkSocial</h1>
        </div>
        <div class="search-bar">
          <input type="text" placeholder="Search something cool..." />
        </div>
      </div>

      <div class="feed">
        <div class="post">
          <h3>@violetdream</h3>
          <p>This sunset is unreal 🌅 #aestheticvibes</p>
          <img
            src="https://images.unsplash.com/photo-1604079621029-19b04a4b1d97?auto=format&fit=crop&w=800&q=80"
            alt="Sunset"
          />
          <div class="post-actions">
            <i class="fas fa-heart"></i>
            <i class="fas fa-comment"></i>
            <i class="fas fa-share"></i>
          </div>
        </div>

        <div class="post">
          <h3>@cyberqueen</h3>
          <p>Late-night coding vibes with neon lights 🔮</p>
          <img
            src="https://images.unsplash.com/photo-1518779578993-ec3579fee39f?auto=format&fit=crop&w=800&q=80"
            alt="Coding setup"
          />
          <div class="post-actions">
            <i class="fas fa-heart"></i>
            <i class="fas fa-comment"></i>
            <i class="fas fa-share"></i>
          </div>
        </div>
      </div>

      <button class="create-btn">
        <i class="fas fa-plus"></i> Create Post
      </button>
    </div>
    <script src="https://cdn.socket.io/4.7.2/socket.io.min.js"></script>
    <script src="/example.js"></script>
    <script>
      const socket = io("http://localhost:3000");
      const sidebar = document.getElementById("sidebar");
      const overlay = document.getElementById("overlay");

      function toggleSidebar() {
        sidebar.classList.toggle("open");
        overlay.classList.toggle("show");
      }

      function closeSidebar() {
        sidebar.classList.remove("open");
        overlay.classList.remove("show");
      }
    </script>
  </body>
</html>
